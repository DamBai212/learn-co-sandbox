const age = 17;

let isAdult, canVote,
canEnlist, canDrink;

if (age >= 16) {
    canVote = true;
 
    if (age >= 18) {
        isAdult = true;
        canEnlist = true;
 
        if (age >= 21) {
            canDrink = true;
        }
    }
}
